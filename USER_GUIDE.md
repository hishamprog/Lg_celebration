# دليل الاستخدام الكامل - نظام استقبال الضيوف 📚

## جدول المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [التثبيت المحلي](#التثبيت-المحلي)
3. [الرفع على GitHub](#الرفع-على-github)
4. [الرفع على Railway](#الرفع-على-railway)
5. [دليل الاستخدام](#دليل-الاستخدام)
6. [الأسئلة الشائعة](#الأسئلة-الشائعة)

---

## نظرة عامة

نظام ويب متكامل لإدارة استقبال الضيوف في الحفلات، يتيح للموظفين:
- ✅ البحث عن الضيوف بسرعة
- ✅ تسجيل الحضور بضغطة زر
- ✅ متابعة الإحصائيات المباشرة
- ✅ عرض آخر الحضور

---

## التثبيت المحلي

### المتطلبات
- Python 3.11 أو أحدث
- pip
- Git (للرفع على GitHub)

### الخطوات

1. **تثبيت المكتبات**
```bash
cd e:\LG_Celebration
pip install -r requirements.txt
```

2. **تشغيل التطبيق**
```bash
python app.py
```

3. **فتح المتصفح**
```
http://localhost:5000
```

### ملاحظات
- قاعدة البيانات تُنشأ تلقائياً عند أول تشغيل
- البيانات تُستورد من ملف Excel تلقائياً
- السيرفر يعمل على المنفذ 5000 افتراضياً

---

## الرفع على GitHub

### الطريقة الأولى: استخدام PowerShell Script

```powershell
cd e:\LG_Celebration
.\setup_git.ps1
```

### الطريقة الثانية: يدوياً

```bash
# 1. تهيئة Git
git init

# 2. إضافة الملفات
git add .

# 3. أول commit
git commit -m "Initial commit: Guest check-in system"

# 4. إضافة الريبو البعيد
git remote add origin https://github.com/hishamprog/Lg_celebration.git

# 5. رفع الكود
git branch -M main
git push -u origin main
```

### إذا كان الريبو موجود مسبقاً

```bash
# حذف المحتوى القديم والرفع من جديد
git remote remove origin
git remote add origin https://github.com/hishamprog/Lg_celebration.git
git push -f origin main
```

---

## الرفع على Railway

### الخطوات السريعة

1. **سجل دخول على Railway**
   - [railway.app](https://railway.app)
   - استخدم حساب GitHub

2. **أنشئ مشروع جديد**
   - New Project → Deploy from GitHub repo
   - اختر: `hishamprog/Lg_celebration`

3. **احصل على الرابط**
   - Settings → Domains → Generate Domain
   - مثال: `https://lg-celebration.up.railway.app`

### التفاصيل الكاملة
راجع ملف [RAILWAY_DEPLOYMENT.md](RAILWAY_DEPLOYMENT.md)

---

## دليل الاستخدام

### للموظفين في الاستقبال

#### 1. البحث عن ضيف

<img src="https://via.placeholder.com/800x400/667eea/ffffff?text=Search+Interface" alt="واجهة البحث" width="100%">

**الخطوات:**
1. أدخل اسم الضيف في خانة البحث
2. اضغط زر "بحث" أو Enter
3. ستظهر النتائج مباشرة

**نصائح:**
- يمكنك البحث بجزء من الاسم
- البحث غير حساس لحالة الأحرف
- النتائج تظهر فوراً

#### 2. تسجيل الحضور

**الخطوات:**
1. ابحث عن الضيف
2. اضغط زر "تسجيل الحضور" بجانب اسمه
3. تحقق من البيانات في النافذة المنبثقة:
   - الاسم
   - رقم الطاولة
   - الموظف المسؤول
4. اضغط "تأكيد الحضور"
5. ستظهر رسالة نجاح مع التفاصيل

**ملاحظات:**
- لا يمكن تسجيل حضور ضيف مرتين
- يتم حفظ وقت الحضور تلقائياً
- يمكنك طباعة كوبون السحب بعد التسجيل

#### 3. متابعة الإحصائيات

**البطاقات الإحصائية:**
- 👥 **إجمالي المدعوين**: العدد الكلي للضيوف
- ✅ **الحضور**: عدد من سجلوا حضورهم
- ⏳ **لم يحضروا**: الباقي

**التحديث:**
- الإحصائيات تتحدث تلقائياً كل 30 ثانية
- يمكنك الضغط على زر "تحديث" للتحديث الفوري

#### 4. آخر الحضور

**المعلومات المعروضة:**
- اسم الضيف
- رقم الطاولة
- الموظف المسؤول
- وقت الحضور

**ملاحظات:**
- يعرض آخر 10 ضيوف
- يتحدث تلقائياً كل 30 ثانية

---

## الأسئلة الشائعة

### التقنية

**س: كيف أغير المنفذ (Port)؟**
```python
# في app.py
port = int(os.environ.get('PORT', 5000))  # غير 5000 للمنفذ المطلوب
```

**س: كيف أضيف موظفين جدد؟**
عدّل في `static/script.js`:
```javascript
checked_by: 'اسم الموظف الجديد'
```

**س: كيف أحذف قاعدة البيانات وأبدأ من جديد؟**
```bash
# احذف ملف قاعدة البيانات
rm guests.db
# شغل التطبيق من جديد
python app.py
```

**س: كيف أضيف ضيوف جدد؟**
- أضفهم في ملف Excel
- احذف قاعدة البيانات
- شغل التطبيق من جديد

### الاستخدام

**س: ماذا لو سجلت حضور ضيف بالخطأ؟**
حالياً لا يمكن التراجع. يمكنك:
1. حذف قاعدة البيانات وإعادة البدء
2. أو تعديل قاعدة البيانات يدوياً

**س: هل يمكن استخدام النظام على أكثر من جهاز؟**
نعم! عند الرفع على Railway:
- جميع الأجهزة تستخدم نفس الرابط
- البيانات متزامنة بين الجميع
- يمكن استخدام موبايل أو تابلت

**س: هل البيانات آمنة؟**
- محلياً: البيانات على جهازك فقط
- على Railway: البيانات على سيرفر Railway
- لا يوجد تشفير حالياً (للاستخدام الداخلي فقط)

### الأخطاء الشائعة

**خطأ: "ModuleNotFoundError: No module named 'flask'"**
```bash
pip install -r requirements.txt
```

**خطأ: "قائمة المدعويين.xlsx not found"**
- تأكد من وجود الملف في نفس المجلد
- تأكد من اسم الملف الصحيح

**خطأ: "Port already in use"**
- أغلق التطبيق القديم
- أو غير المنفذ في الكود

---

## التخصيص

### تغيير الألوان

في `static/style.css`:
```css
:root {
    --primary: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    --success: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
}
```

### تغيير الخط

في `templates/index.html`:
```html
<link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@300;400;600;700;900&display=swap" rel="stylesheet">
```

ثم في `static/style.css`:
```css
body {
    font-family: 'Tajawal', sans-serif;
}
```

### إضافة شعار الشركة

في `templates/index.html`:
```html
<h1 class="logo">
    <img src="/static/logo.png" alt="Logo" style="height: 40px;">
    LG Celebration
</h1>
```

---

## الدعم والمساعدة

### الملفات المرجعية
- [README.md](README.md) - نظرة عامة
- [GIT_SETUP.md](GIT_SETUP.md) - إعداد Git
- [RAILWAY_DEPLOYMENT.md](RAILWAY_DEPLOYMENT.md) - الرفع على Railway

### المساهمة
نرحب بالمساهمات! افتح Issue أو Pull Request على GitHub.

### الترخيص
مفتوح المصدر - استخدم بحرية

---

**صُنع بـ ❤️ للحفلات الناجحة**

🎉 **LG Celebration** - نظام استقبال الضيوف الاحترافي
